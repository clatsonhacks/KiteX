{
  "name": "kitex-onchain",
  "version": "1.0.0",
  "description": "Kitex smart contracts - CapitalRouter and KitexAuditLog",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.ts --network kite",
    "verify:capital": "hardhat verify --network kite $CAPITAL_ROUTER_ADDRESS",
    "verify:audit": "hardhat verify --network kite $AUDIT_LOG_ADDRESS"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/node": "^20.0.0",
    "hardhat": "^2.22.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.0.0"
  }
}
